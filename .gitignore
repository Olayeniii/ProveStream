node_modules/
dist/
coverage/
*.tsbuildinfo
.env
.env.local
*.log

# Hardhat
artifacts/
cache/
ignition/deployments/

# Editors and operating systems
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

Documents/
.github/instructions/
.github/copilot-instructions.md

dashboardview.png

# Ad-hoc root-level screenshots from manual/Playwright verification passes
/*.png
.playwright-mcp/

# Local-only cache of the backend's read models + chain-scan progress, so a
# restart doesn't need to re-scan full history from each contract's
# deployment block (see apps/backend/src/services/snapshotStore.ts).
apps/backend/.store-snapshot.json

# Demo video tooling (Remotion) — standalone, not an npm workspace member,
# local-only, not tracked.
/demo-video/

# circom/snarkjs build output (compiled circuit, trusted-setup files, test
# witnesses/proofs) — regeneratable from circuits/thresholdCheck.circom via
# `circom` + `snarkjs`, same reasoning as artifacts/ above. Note:
# thresholdCheck_final.zkey specifically is the actual proving key matching
# the verifier already deployed on Arc testnet — regenerating the trusted
# setup produces a different zkey that won't match that deployment, so keep
# a copy of it somewhere safe before deleting this directory.
circuits/build/